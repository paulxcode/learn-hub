{
  "builds": [
    { "src": "server.js", "use": "@vercel/node", "config": { "includeFiles": ["content/**", "public/**", "database/schema.sql"] } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server.js" }
  ]
}
